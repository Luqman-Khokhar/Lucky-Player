import type { ReactNode } from 'react';

export type LibrarySectionProps = {
  /** Segmented control rendered under the header when the screen is a section of the Videos tab. */
  segments?: ReactNode;
};
